{
  "schema_version": "1",
  "issue": 0,
  "_about": "VERIFIED storefront render: a variant product in a STATIC product-slider on a landing page (the issue-#30 family — 'X not displayed in CMS product slider'). executor=playwright (visual). Copy fixtures.json's NESTED cms_page→sections→blocks→slots verbatim — hand-rolling flat cms_section/cms_block/cms_slot with the wrong FK names is the #1 way these runs fail. Reach the page at `/landingPage/<id>` (301-redirects to the SEO url). A variant parent in a static slider renders ONE card linking to the PARENT — the slider does not expand variants.",
  "layer": "storefront",
  "executor": "playwright",
  "version": "selfcheck",
  "build_profile": { "admin_build": false, "storefront_build": true, "theme_build": true },
  "fixtures": { "demodata": false, "sync_payload_path": "fixtures.json" },
  "script_path": "repro.spec.ts",
  "confidence": 1
}
