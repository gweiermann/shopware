{
  "schema_version": "1",
  "issue": 0,
  "_about": "product + variants in a category listing — preselected (white) main variant, configurator, visibility, indexing. Covers listing / slider / variant-preselection / availability / property-filter bugs. Option text is at `.elements[].options[].name` (`.variation` is null over http).",
  "layer": "store-api",
  "executor": "http",
  "version": "selfcheck",
  "build_profile": { "admin_build": false, "storefront_build": false, "theme_build": false },
  "fixtures": { "demodata": false, "sync_payload_path": "fixtures.json" },
  "request": { "method": "POST", "path": "/store-api/product-listing/{{NAV_CAT}}", "headers": {}, "body": "{}" },
  "assertions": [
    { "role": "precondition", "kind": "http_status", "expect": "200" },
    { "role": "precondition", "field": "[.elements[].productNumber] | index(\"REPRO-003-WHITE\")", "op": "present" },
    { "field": ".elements[] | select(.productNumber==\"REPRO-003-WHITE\") | .options[0].name", "op": "equals", "expect": "white" }
  ],
  "confidence": 1
}
